# Python script CLAUDE.md

For a single file Python utility. No packaging, no venv juggling, just a script that does one job well. Copy `CLAUDE.md` into the folder, tweak the top section, run `claude`.
